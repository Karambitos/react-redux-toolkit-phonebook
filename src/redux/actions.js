import { createAction } from '@reduxjs/toolkit';

export const createContact = createAction('createContact');
export const deliteContact = createAction('deliteContact');
export const filterContact = createAction('filterContact');
